import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Progress } from "@/components/ui/progress";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";
import { Toaster } from "@/components/ui/sonner";
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@/components/ui/table";
import { Textarea } from "@/components/ui/textarea";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import {
  Activity,
  AlertTriangle,
  BarChart3,
  Brain,
  CheckCircle2,
  ChevronRight,
  Clock,
  FileText,
  History,
  Loader2,
  LogOut,
  Search,
  Settings,
  Shield,
  Upload,
  Zap,
} from "lucide-react";
import { AnimatePresence, motion } from "motion/react";
import type React from "react";
import { useCallback, useRef, useState } from "react";
import {
  Bar,
  BarChart,
  Cell,
  ResponsiveContainer,
  Tooltip,
  XAxis,
  YAxis,
} from "recharts";
import { toast } from "sonner";
import { ExternalBlob, Modality, Quality, TriageStatus } from "./backend";
import type { AnalysisRecord } from "./backend";
import { ScanViewer } from "./components/ScanViewer";
import { useActor } from "./hooks/useActor";
import { useInternetIdentity } from "./hooks/useInternetIdentity";

// ─── Types ────────────────────────────────────────────────────────────────────

type AppView = "upload" | "analysis" | "history" | "triage" | "hospital";

type ModalityUI = "xray" | "ct" | "mri";

interface BBox {
  x: number;
  y: number;
  w: number;
  h: number;
}

interface FindingResult {
  name: string;
  confidence: number;
  bbox: BBox;
}

interface AgentStep {
  name: string;
  status: "idle" | "processing" | "complete" | "error";
  icon: React.ReactNode;
}

interface AnalysisResult {
  quality: Quality;
  qualityReason: string;
  findings: FindingResult[];
  findingsSummary: string;
  triageStatus: TriageStatus;
  triageReason: string;
  priority: string;
  reportText: string;
  submittedId?: bigint;
}

// ─── Helpers ──────────────────────────────────────────────────────────────────

const modalityToBackend = (m: ModalityUI): Modality =>
  m === "mri" ? Modality.mri : Modality.ct;

const modalityLabel = (m: ModalityUI | string): string => {
  if (m === "xray") return "X-Ray";
  if (m === "ct") return "CT Scan";
  if (m === "mri") return "MRI";
  return m.toUpperCase();
};

const caseId = (id: bigint | number) => `RA-${String(id).padStart(5, "0")}`;

const formatDate = (ts: bigint) => {
  const ms = Number(ts) / 1_000_000;
  return new Date(ms).toLocaleDateString("en-US", {
    month: "short",
    day: "numeric",
    year: "numeric",
  });
};

// ─── AI API ───────────────────────────────────────────────────────────────

const GEMINI_URL =
  "https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent";

async function geminiTextCall(apiKey: string, prompt: string): Promise<string> {
  const res = await fetch(`${GEMINI_URL}?key=${apiKey}`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      contents: [{ parts: [{ text: prompt }] }],
    }),
  });
  if (!res.ok) throw new Error(`API error ${res.status}`);
  const data = await res.json();
  return data.candidates[0].content.parts[0].text as string;
}

async function geminiImageCall(
  apiKey: string,
  prompt: string,
  base64: string,
): Promise<string> {
  const res = await fetch(`${GEMINI_URL}?key=${apiKey}`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      contents: [
        {
          parts: [
            { text: prompt },
            { inline_data: { mime_type: "image/jpeg", data: base64 } },
          ],
        },
      ],
    }),
  });
  if (!res.ok) throw new Error(`API error ${res.status}`);
  const data = await res.json();
  return data.candidates[0].content.parts[0].text as string;
}

function safeJsonParse<T>(text: string): T | null {
  try {
    // Strip markdown code blocks if present
    const cleaned = text.replace(/```(?:json)?\n?/g, "").trim();
    return JSON.parse(cleaned) as T;
  } catch {
    return null;
  }
}

const MOCK_RESULTS: AnalysisResult[] = [
  // 1. Femur fracture
  {
    quality: Quality.good,
    qualityReason: "Clear radiograph with adequate exposure and positioning",
    findings: [
      {
        name: "Transverse Femoral Shaft Fracture",
        confidence: 96,
        bbox: { x: 42, y: 35, w: 16, h: 30 },
      },
      {
        name: "Cortical Disruption with Displacement",
        confidence: 91,
        bbox: { x: 40, y: 33, w: 20, h: 34 },
      },
      {
        name: "Periosteal Elevation",
        confidence: 72,
        bbox: { x: 38, y: 30, w: 24, h: 40 },
      },
      {
        name: "Soft Tissue Swelling",
        confidence: 67,
        bbox: { x: 30, y: 25, w: 40, h: 50 },
      },
    ],
    findingsSummary:
      "Acute displaced transverse femoral shaft fracture with periosteal reaction",
    triageStatus: TriageStatus.critical,
    triageReason:
      "Displaced long bone fracture requiring urgent orthopedic intervention",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nTrauma evaluation — right thigh pain and deformity after fall.\n\nFINDINGS:\n1. Transverse fracture of mid-femoral shaft with approximately 8mm lateral displacement (96% confidence).\n2. Cortical disruption with angulation at fracture site (91% confidence).\n3. Periosteal elevation adjacent to fracture (72% confidence).\n4. Soft tissue swelling overlying the fracture region (67% confidence).\n\nIMPRESSION:\nAcute displaced transverse femoral shaft fracture. Urgent orthopedic consultation and surgical fixation recommended. Evaluate for vascular injury given degree of displacement.",
  },
  // 2. Wrist fracture (Colles)
  {
    quality: Quality.good,
    qualityReason: "AP and lateral views adequate for fracture assessment",
    findings: [
      {
        name: "Distal Radius Fracture (Colles Type)",
        confidence: 95,
        bbox: { x: 44, y: 55, w: 18, h: 22 },
      },
      {
        name: "Dorsal Angulation ~20°",
        confidence: 88,
        bbox: { x: 42, y: 53, w: 22, h: 26 },
      },
      {
        name: "Ulnar Styloid Avulsion Fracture",
        confidence: 79,
        bbox: { x: 60, y: 58, w: 12, h: 14 },
      },
    ],
    findingsSummary:
      "Colles fracture with dorsal angulation and ulnar styloid avulsion",
    triageStatus: TriageStatus.critical,
    triageReason: "Displaced distal radius fracture requiring reduction",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nFall on outstretched hand — wrist pain and swelling.\n\nFINDINGS:\n1. Transverse fracture of the distal radius with dorsal angulation of approximately 20° (Colles fracture pattern) (95% confidence).\n2. Dorsal tilt and radial shortening noted (88% confidence).\n3. Avulsion fracture at the ulnar styloid tip (79% confidence).\n\nIMPRESSION:\nColles fracture with ulnar styloid avulsion. Orthopedic evaluation for closed reduction and immobilization recommended.",
  },
  // 3. Spine — compression fracture
  {
    quality: Quality.good,
    qualityReason: "Lateral lumbar spine view of diagnostic quality",
    findings: [
      {
        name: "L1 Vertebral Compression Fracture",
        confidence: 93,
        bbox: { x: 43, y: 42, w: 14, h: 12 },
      },
      {
        name: "Anterior Wedging (~30%)",
        confidence: 87,
        bbox: { x: 42, y: 41, w: 16, h: 14 },
      },
      {
        name: "End-plate Depression",
        confidence: 80,
        bbox: { x: 43, y: 43, w: 14, h: 10 },
      },
      {
        name: "Mild Kyphotic Deformity",
        confidence: 73,
        bbox: { x: 35, y: 30, w: 30, h: 40 },
      },
    ],
    findingsSummary:
      "L1 compression fracture with anterior wedging and kyphotic angulation",
    triageStatus: TriageStatus.critical,
    triageReason:
      "Vertebral compression fracture with potential spinal cord risk",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nAcute back pain following minor trauma in elderly patient.\n\nFINDINGS:\n1. Compression fracture of L1 vertebral body with approximately 30% anterior height loss (93% confidence).\n2. Anterior wedging creating focal kyphotic deformity (87% confidence).\n3. Superior end-plate depression (80% confidence).\n4. Mild regional kyphosis (73% confidence). No posterior wall retropulsion identified.\n\nIMPRESSION:\nAcute L1 compression fracture, likely osteoporotic. MRI recommended to evaluate for bone marrow edema, posterior element involvement, and cord status.",
  },
  // 4. Pneumothorax
  {
    quality: Quality.good,
    qualityReason: "Upright PA chest radiograph of adequate quality",
    findings: [
      {
        name: "Left Apical Pneumothorax",
        confidence: 97,
        bbox: { x: 10, y: 5, w: 35, h: 30 },
      },
      {
        name: "Pleural Line Visible",
        confidence: 93,
        bbox: { x: 12, y: 7, w: 30, h: 25 },
      },
      {
        name: "Absent Lung Markings Left Upper Zone",
        confidence: 89,
        bbox: { x: 8, y: 5, w: 38, h: 35 },
      },
    ],
    findingsSummary: "Left-sided pneumothorax with visible pleural line",
    triageStatus: TriageStatus.critical,
    triageReason:
      "Pneumothorax requires immediate intervention to prevent tension",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nChest pain and dyspnea — evaluate for pneumothorax.\n\nFINDINGS:\n1. Visible pleural line in the left upper zone consistent with pneumothorax (97% confidence). Estimated size ~20%.\n2. Absent lung markings peripheral to pleural line (89% confidence).\n3. No obvious mediastinal shift at this time.\n\nIMPRESSION:\nLeft-sided pneumothorax (~20%). Urgent clinical assessment required. Chest tube insertion likely indicated. Monitor for tension pneumothorax.",
  },
  // 5. Pulmonary embolism (CT)
  {
    quality: Quality.good,
    qualityReason:
      "CT pulmonary angiography with adequate contrast opacification",
    findings: [
      {
        name: "Filling Defect — Right Main Pulmonary Artery",
        confidence: 95,
        bbox: { x: 48, y: 40, w: 18, h: 16 },
      },
      {
        name: "Saddle Embolus at Bifurcation",
        confidence: 90,
        bbox: { x: 44, y: 38, w: 22, h: 18 },
      },
      {
        name: "Right Heart Strain (RV Dilatation)",
        confidence: 82,
        bbox: { x: 52, y: 48, w: 22, h: 20 },
      },
    ],
    findingsSummary: "Saddle pulmonary embolus with right heart strain",
    triageStatus: TriageStatus.critical,
    triageReason: "Massive pulmonary embolism — life-threatening emergency",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nCT pulmonary angiography for suspected pulmonary embolism.\n\nFINDINGS:\n1. Central filling defect at the pulmonary artery bifurcation consistent with saddle embolus (95% confidence).\n2. Extension into the right main pulmonary artery (90% confidence).\n3. Right ventricular dilatation (RV:LV ratio >1.0) indicating right heart strain (82% confidence).\n\nIMPRESSION:\nMassive pulmonary embolism with saddle configuration and right heart strain. Immediate anticoagulation and cardiology/pulmonology consultation required. Consider thrombolysis.",
  },
  // 6. Pneumonia
  {
    quality: Quality.good,
    qualityReason: "Image is clear and suitable for analysis",
    findings: [
      {
        name: "Right Lower Lobe Consolidation",
        confidence: 94,
        bbox: { x: 55, y: 45, w: 25, h: 30 },
      },
      {
        name: "Air Bronchograms",
        confidence: 85,
        bbox: { x: 57, y: 47, w: 20, h: 25 },
      },
      {
        name: "Pleural Effusion (Right)",
        confidence: 81,
        bbox: { x: 60, y: 62, w: 20, h: 18 },
      },
    ],
    findingsSummary:
      "Right lower lobe consolidation with air bronchograms and pleural effusion",
    triageStatus: TriageStatus.critical,
    triageReason: "Lobar pneumonia with parapneumonic effusion",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nFever, productive cough, and hypoxia.\n\nFINDINGS:\n1. Homogeneous consolidation of the right lower lobe with air bronchograms (94% confidence) — consistent with lobar pneumonia.\n2. Air bronchograms within consolidation (85% confidence).\n3. Small right-sided parapneumonic pleural effusion (81% confidence).\n\nIMPRESSION:\nRight lower lobe pneumonia with parapneumonic effusion. Antibiotic therapy and follow-up imaging in 6–8 weeks recommended.",
  },
  // 7. Brain hemorrhage (CT)
  {
    quality: Quality.good,
    qualityReason: "Non-contrast CT brain of adequate diagnostic quality",
    findings: [
      {
        name: "Right Basal Ganglia Hemorrhage",
        confidence: 97,
        bbox: { x: 52, y: 38, w: 18, h: 18 },
      },
      {
        name: "Perilesional Hypodense Edema",
        confidence: 88,
        bbox: { x: 48, y: 33, w: 26, h: 26 },
      },
      {
        name: "Midline Shift (~5mm Left)",
        confidence: 84,
        bbox: { x: 40, y: 30, w: 20, h: 40 },
      },
      {
        name: "Intraventricular Extension",
        confidence: 76,
        bbox: { x: 43, y: 42, w: 14, h: 14 },
      },
    ],
    findingsSummary:
      "Right basal ganglia hypertensive hemorrhage with midline shift",
    triageStatus: TriageStatus.critical,
    triageReason:
      "Intracranial hemorrhage with mass effect — neurosurgical emergency",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nAcute onset hemiplegia and altered consciousness.\n\nFINDINGS:\n1. Hyperdense (60 HU) right basal ganglia hematoma measuring approximately 3.5 × 3.0 cm (97% confidence).\n2. Perilesional hypodense edema (88% confidence).\n3. Midline shift of approximately 5mm to the left (84% confidence).\n4. Early intraventricular extension into the right lateral ventricle (76% confidence).\n\nIMPRESSION:\nAcute right basal ganglia hemorrhage — likely hypertensive etiology. Midline shift and intraventricular extension noted. Urgent neurosurgery consultation and ICU admission required.",
  },
  // 8. Ischemic stroke (MRI DWI)
  {
    quality: Quality.good,
    qualityReason: "MRI DWI/ADC sequences of diagnostic quality",
    findings: [
      {
        name: "Restricted Diffusion — Left MCA Territory",
        confidence: 94,
        bbox: { x: 20, y: 28, w: 32, h: 28 },
      },
      {
        name: "ADC Map Hypointensity",
        confidence: 91,
        bbox: { x: 21, y: 29, w: 30, h: 26 },
      },
      {
        name: "FLAIR Hyperintensity (Cortical)",
        confidence: 83,
        bbox: { x: 22, y: 30, w: 28, h: 24 },
      },
    ],
    findingsSummary: "Acute left MCA territory infarct on DWI",
    triageStatus: TriageStatus.critical,
    triageReason: "Acute ischemic stroke — time-critical for thrombolysis",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nAcute right-sided weakness and aphasia — rule out stroke.\n\nFINDINGS:\n1. Restricted diffusion in the left middle cerebral artery territory (94% confidence) — consistent with acute infarction.\n2. Corresponding ADC map hypointensity confirming cytotoxic edema (91% confidence).\n3. Early FLAIR cortical signal change suggesting established component (83% confidence).\n\nIMPRESSION:\nAcute left MCA territory ischemic stroke. Urgent stroke team activation. Evaluate for IV tPA or mechanical thrombectomy candidacy.",
  },
  // 9. Liver mass (CT abdomen)
  {
    quality: Quality.good,
    qualityReason: "Contrast-enhanced CT abdomen of adequate quality",
    findings: [
      {
        name: "Hepatic Mass — Right Lobe Segment VII",
        confidence: 91,
        bbox: { x: 55, y: 35, w: 22, h: 22 },
      },
      {
        name: "Arterial Hyperenhancement",
        confidence: 86,
        bbox: { x: 56, y: 36, w: 20, h: 20 },
      },
      {
        name: "Washout Appearance (Portal Phase)",
        confidence: 82,
        bbox: { x: 55, y: 35, w: 22, h: 22 },
      },
      {
        name: "No Satellite Lesions Identified",
        confidence: 90,
        bbox: { x: 10, y: 10, w: 80, h: 80 },
      },
    ],
    findingsSummary:
      "Hepatic mass with LI-RADS 5 features (arterial enhancement + washout)",
    triageStatus: TriageStatus.critical,
    triageReason:
      "LI-RADS 5 lesion — hepatocellular carcinoma highly suspected",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nElevated AFP and cirrhosis — evaluate for hepatocellular carcinoma.\n\nFINDINGS:\n1. 3.2 cm hypervascular lesion in right hepatic lobe segment VII showing arterial phase enhancement (91% confidence).\n2. Washout appearance on portal venous phase (82% confidence).\n3. No satellite lesions or portal vein thrombus identified.\n4. Background liver shows coarsened echotexture consistent with cirrhosis.\n\nIMPRESSION:\nLI-RADS 5 hepatic lesion in segment VII — highly suspicious for hepatocellular carcinoma. Multidisciplinary oncology and hepatology review recommended. Stage with chest CT.",
  },
  // 10. Kidney stone (CT KUB)
  {
    quality: Quality.good,
    qualityReason: "Non-contrast CT KUB of adequate diagnostic quality",
    findings: [
      {
        name: "Right Distal Ureteric Calculus (5mm)",
        confidence: 96,
        bbox: { x: 58, y: 62, w: 10, h: 10 },
      },
      {
        name: "Proximal Ureteric Dilatation",
        confidence: 88,
        bbox: { x: 57, y: 35, w: 8, h: 30 },
      },
      {
        name: "Perinephric Stranding",
        confidence: 80,
        bbox: { x: 52, y: 28, w: 20, h: 22 },
      },
    ],
    findingsSummary:
      "Right distal ureteric stone with hydronephrosis and perinephric stranding",
    triageStatus: TriageStatus.critical,
    triageReason: "Obstructing ureteric calculus causing hydronephrosis",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nAcute right flank pain and haematuria.\n\nFINDINGS:\n1. 5mm hyperdense calculus at the right vesicoureteric junction (96% confidence).\n2. Moderate right hydroureteronephrosis proximal to the calculus (88% confidence).\n3. Perinephric fat stranding indicating obstruction-related inflammation (80% confidence).\n\nIMPRESSION:\nObstructing right distal ureteric calculus with moderate hydronephrosis. Urology consultation recommended. Medical expulsive therapy initiation and monitoring of renal function advised.",
  },
  // 11. Normal lumbar spine
  {
    quality: Quality.good,
    qualityReason: "AP and lateral lumbar views of good diagnostic quality",
    findings: [
      {
        name: "Normal Vertebral Alignment",
        confidence: 96,
        bbox: { x: 35, y: 15, w: 30, h: 70 },
      },
      {
        name: "Preserved Disc Spaces L1–S1",
        confidence: 93,
        bbox: { x: 38, y: 20, w: 24, h: 60 },
      },
    ],
    findingsSummary: "No significant abnormality identified in lumbar spine",
    triageStatus: TriageStatus.normal,
    triageReason: "Normal study with no acute or chronic pathology",
    priority: "Standard",
    reportText:
      "CLINICAL INDICATION:\nLow back pain — evaluate lumbar spine.\n\nFINDINGS:\n1. Normal vertebral body heights and alignment throughout L1–S1.\n2. Preserved intervertebral disc spaces at all levels.\n3. No fracture, listhesis, or significant degenerative change identified.\n4. Posterior elements and sacrum appear intact.\n\nIMPRESSION:\nNormal lumbar spine radiograph. No acute or significant chronic pathology identified. Clinical correlation recommended.",
  },
  // 12. Degenerative disc disease
  {
    quality: Quality.good,
    qualityReason: "Lateral cervical spine view of adequate quality",
    findings: [
      {
        name: "Disc Space Narrowing C5–C6",
        confidence: 91,
        bbox: { x: 42, y: 48, w: 16, h: 10 },
      },
      {
        name: "Anterior Osteophytes C5–C6",
        confidence: 85,
        bbox: { x: 40, y: 46, w: 12, h: 14 },
      },
      {
        name: "Disc Space Narrowing C6–C7",
        confidence: 80,
        bbox: { x: 42, y: 58, w: 16, h: 10 },
      },
      {
        name: "Loss of Cervical Lordosis",
        confidence: 74,
        bbox: { x: 35, y: 20, w: 30, h: 65 },
      },
    ],
    findingsSummary: "Multi-level cervical spondylosis with loss of lordosis",
    triageStatus: TriageStatus.normal,
    triageReason: "Degenerative disease, no acute emergency",
    priority: "Standard",
    reportText:
      "CLINICAL INDICATION:\nNeck pain and upper limb tingling — cervical spine evaluation.\n\nFINDINGS:\n1. Disc space narrowing at C5–C6 and C6–C7 (91% and 80% confidence respectively).\n2. Anterior osteophytes at C5–C6 (85% confidence).\n3. Straightening/loss of normal cervical lordosis (74% confidence).\n\nIMPRESSION:\nMild to moderate cervical spondylosis at C5–C6 and C6–C7. No fracture or significant canal stenosis identified on plain film. MRI recommended if radiculopathy is clinically suspected.",
  },
  // 13. Aortic aneurysm (CT)
  {
    quality: Quality.good,
    qualityReason: "CT aorta with adequate contrast opacification",
    findings: [
      {
        name: "Infrarenal Aortic Aneurysm (6.2cm)",
        confidence: 97,
        bbox: { x: 44, y: 40, w: 12, h: 22 },
      },
      {
        name: "Mural Thrombus",
        confidence: 88,
        bbox: { x: 44, y: 41, w: 12, h: 20 },
      },
      {
        name: "No Rupture Signs",
        confidence: 92,
        bbox: { x: 10, y: 10, w: 80, h: 80 },
      },
    ],
    findingsSummary:
      "Large infrarenal AAA (6.2cm) with mural thrombus — no rupture",
    triageStatus: TriageStatus.critical,
    triageReason:
      "AAA >5.5cm exceeds surgical threshold — urgent vascular review",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nCT aorta — known aneurysm surveillance.\n\nFINDINGS:\n1. Infrarenal abdominal aortic aneurysm measuring 6.2 × 5.9 cm (97% confidence) — increased from prior measurement.\n2. Mural thrombus lining the aneurysm sac (88% confidence).\n3. No periaortic haematoma, retroperitoneal fat stranding, or extravasation to suggest rupture (92% confidence).\n4. Aneurysm neck begins 1.5 cm below the renal arteries.\n\nIMPRESSION:\nLarge infrarenal AAA at 6.2cm exceeding the 5.5cm surgical threshold. Urgent vascular surgery consultation for EVAR/open repair planning.",
  },
  // 14. Knee osteoarthritis
  {
    quality: Quality.good,
    qualityReason: "Weight-bearing AP and lateral knee views of good quality",
    findings: [
      {
        name: "Medial Joint Space Narrowing (Grade 3)",
        confidence: 92,
        bbox: { x: 42, y: 50, w: 16, h: 10 },
      },
      {
        name: "Subchondral Sclerosis",
        confidence: 86,
        bbox: { x: 40, y: 48, w: 20, h: 14 },
      },
      {
        name: "Marginal Osteophytes (Medial)",
        confidence: 83,
        bbox: { x: 38, y: 52, w: 10, h: 12 },
      },
      {
        name: "Varus Deformity",
        confidence: 77,
        bbox: { x: 30, y: 20, w: 40, h: 70 },
      },
    ],
    findingsSummary:
      "Severe medial compartment osteoarthritis with varus deformity",
    triageStatus: TriageStatus.normal,
    triageReason: "Chronic degenerative condition, no acute emergency",
    priority: "Standard",
    reportText:
      "CLINICAL INDICATION:\nRight knee pain and stiffness — evaluate for arthritis.\n\nFINDINGS:\n1. Severe medial compartment joint space narrowing (Grade 3/4 Kellgren-Lawrence) (92% confidence).\n2. Subchondral sclerosis and cystic change in medial tibial plateau (86% confidence).\n3. Medial marginal osteophytes (83% confidence).\n4. Varus deformity of approximately 8° (77% confidence).\n\nIMPRESSION:\nSevere right medial compartment osteoarthritis with varus deformity. Orthopaedic referral for surgical management (total knee arthroplasty) recommended.",
  },
  // 15. Hip fracture
  {
    quality: Quality.good,
    qualityReason: "AP pelvis and lateral hip views of adequate quality",
    findings: [
      {
        name: "Displaced Femoral Neck Fracture (Garden III)",
        confidence: 95,
        bbox: { x: 38, y: 45, w: 18, h: 16 },
      },
      {
        name: "Varus Angulation of Femoral Head",
        confidence: 88,
        bbox: { x: 36, y: 43, w: 22, h: 20 },
      },
      {
        name: "Disrupted Shenton's Line",
        confidence: 84,
        bbox: { x: 30, y: 40, w: 30, h: 25 },
      },
    ],
    findingsSummary: "Garden III displaced femoral neck fracture",
    triageStatus: TriageStatus.critical,
    triageReason:
      "Displaced hip fracture — high risk of avascular necrosis, requires surgery",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nElderly patient with acute hip pain and inability to weight-bear after fall.\n\nFINDINGS:\n1. Displaced femoral neck fracture classified as Garden type III (95% confidence).\n2. Varus angulation of the femoral head fragment (88% confidence).\n3. Disrupted Shenton's line confirming displacement (84% confidence).\n\nIMPRESSION:\nGarden III displaced femoral neck fracture. High risk of avascular necrosis of femoral head. Urgent orthopaedic consultation for hemiarthroplasty or total hip replacement.",
  },
  // 16. Appendicitis (CT)
  {
    quality: Quality.good,
    qualityReason: "CT abdomen/pelvis with adequate contrast",
    findings: [
      {
        name: "Dilated Appendix (10mm diameter)",
        confidence: 94,
        bbox: { x: 58, y: 58, w: 14, h: 18 },
      },
      {
        name: "Periappendiceal Fat Stranding",
        confidence: 89,
        bbox: { x: 54, y: 54, w: 22, h: 26 },
      },
      {
        name: "Appendicolith",
        confidence: 82,
        bbox: { x: 59, y: 56, w: 8, h: 8 },
      },
    ],
    findingsSummary:
      "Acute appendicitis with appendicolith and periappendiceal inflammation",
    triageStatus: TriageStatus.critical,
    triageReason: "Acute appendicitis — surgical emergency",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nRight iliac fossa pain, fever, elevated WBC — rule out appendicitis.\n\nFINDINGS:\n1. Dilated appendix measuring 10mm in diameter with wall thickening (94% confidence).\n2. Periappendiceal fat stranding and free fluid (89% confidence).\n3. Appendicolith at the base of the appendix (82% confidence).\n4. No perforation or abscess identified.\n\nIMPRESSION:\nAcute appendicitis with appendicolith. Surgical consultation for appendicectomy urgently required.",
  },
  // 17. Normal brain MRI
  {
    quality: Quality.good,
    qualityReason: "Multisequence MRI brain of good diagnostic quality",
    findings: [
      {
        name: "Normal Cerebral Parenchyma",
        confidence: 97,
        bbox: { x: 15, y: 15, w: 70, h: 70 },
      },
      {
        name: "Normal Ventricular System",
        confidence: 95,
        bbox: { x: 38, y: 35, w: 24, h: 30 },
      },
    ],
    findingsSummary: "No intracranial abnormality detected",
    triageStatus: TriageStatus.normal,
    triageReason: "Normal study",
    priority: "Standard",
    reportText:
      "CLINICAL INDICATION:\nHeadache — exclude intracranial pathology.\n\nFINDINGS:\n1. Normal signal intensity of cerebral cortex, white matter, and deep grey nuclei on all sequences.\n2. Ventricular system normal in size and morphology. No midline shift.\n3. No focal lesion, haemorrhage, infarction, or abnormal enhancement.\n4. Posterior fossa structures and cranial nerves appear normal.\n\nIMPRESSION:\nNormal MRI brain. No intracranial pathology identified.",
  },
  // 18. Pleural effusion
  {
    quality: Quality.good,
    qualityReason: "PA chest radiograph of adequate quality",
    findings: [
      {
        name: "Large Left Pleural Effusion",
        confidence: 96,
        bbox: { x: 8, y: 30, w: 38, h: 50 },
      },
      {
        name: "Blunting of Left Costophrenic Angle",
        confidence: 93,
        bbox: { x: 10, y: 68, w: 30, h: 18 },
      },
      {
        name: "Mediastinal Shift to Right",
        confidence: 85,
        bbox: { x: 40, y: 25, w: 20, h: 50 },
      },
      {
        name: "Left Lower Lobe Compressive Atelectasis",
        confidence: 80,
        bbox: { x: 12, y: 50, w: 30, h: 30 },
      },
    ],
    findingsSummary:
      "Large left pleural effusion with mediastinal shift and compressive atelectasis",
    triageStatus: TriageStatus.critical,
    triageReason: "Tension effusion causing mediastinal shift",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nProgressive dyspnea and dullness to percussion.\n\nFINDINGS:\n1. Large left pleural effusion with near-complete opacification of the left hemithorax (96% confidence).\n2. Blunting of the left costophrenic angle (93% confidence).\n3. Mediastinal shift to the right (85% confidence) suggesting tension physiology.\n4. Compressive atelectasis of the left lower lobe (80% confidence).\n\nIMPRESSION:\nLarge left pleural effusion with mediastinal shift. Urgent thoracocentesis required to relieve tension and obtain fluid for analysis.",
  },
  // 19. Shoulder dislocation
  {
    quality: Quality.good,
    qualityReason: "AP and axillary shoulder views of adequate quality",
    findings: [
      {
        name: "Anterior Glenohumeral Dislocation",
        confidence: 97,
        bbox: { x: 35, y: 38, w: 28, h: 26 },
      },
      {
        name: "Hill-Sachs Deformity",
        confidence: 82,
        bbox: { x: 50, y: 32, w: 16, h: 16 },
      },
      {
        name: "No Associated Fracture",
        confidence: 88,
        bbox: { x: 20, y: 20, w: 60, h: 60 },
      },
    ],
    findingsSummary: "Anterior shoulder dislocation with Hill-Sachs deformity",
    triageStatus: TriageStatus.critical,
    triageReason: "Joint dislocation requiring urgent reduction",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nTrauma — shoulder pain and deformity after fall on outstretched hand.\n\nFINDINGS:\n1. Anterior glenohumeral dislocation with humeral head displaced anteriorly and inferiorly to the glenoid (97% confidence).\n2. Hill-Sachs deformity (posterolateral humeral head compression fracture) (82% confidence).\n3. No associated greater tuberosity or glenoid rim fracture identified (88% confidence).\n\nIMPRESSION:\nAnterior shoulder dislocation with Hill-Sachs lesion. Urgent closed reduction under sedation/analgesia recommended. Post-reduction imaging required.",
  },
  // 21. Brain tumor (MRI)
  {
    quality: Quality.good,
    qualityReason:
      "Multisequence MRI brain with and without contrast of diagnostic quality",
    findings: [
      {
        name: "Ring-Enhancing Mass — Right Frontal Lobe",
        confidence: 96,
        bbox: { x: 52, y: 22, w: 24, h: 24 },
      },
      {
        name: "Perilesional Vasogenic Edema",
        confidence: 91,
        bbox: { x: 46, y: 16, w: 36, h: 36 },
      },
      {
        name: "Mass Effect with Sulcal Effacement",
        confidence: 87,
        bbox: { x: 50, y: 18, w: 30, h: 30 },
      },
      {
        name: "Midline Shift (~4mm Left)",
        confidence: 83,
        bbox: { x: 40, y: 30, w: 20, h: 40 },
      },
    ],
    findingsSummary:
      "Right frontal ring-enhancing mass with perilesional edema — high-grade glioma or metastasis",
    triageStatus: TriageStatus.critical,
    triageReason:
      "Intracranial mass with mass effect — urgent neurosurgical and neuro-oncology review required",
    priority: "Critical-first",
    reportText:
      "CLINICAL INDICATION:\nNew-onset focal seizures and progressive right-sided headache.\n\nFINDINGS:\n1. Heterogeneously enhancing ring-enhancing mass in the right frontal lobe measuring approximately 3.2 × 2.8 × 3.0 cm (96% confidence).\n2. Extensive surrounding T2/FLAIR hyperintense vasogenic edema involving the right frontal and parietal lobes (91% confidence).\n3. Local mass effect with sulcal effacement and subfalcine herniation (87% confidence).\n4. Midline shift of approximately 4mm to the left (83% confidence).\n5. No additional enhancing lesions to suggest multifocal disease.\n\nIMPRESSION:\nLarge ring-enhancing right frontal mass with significant perilesional edema and midline shift. Differential includes high-grade glioma (GBM) vs. solitary metastasis. Urgent neurosurgical consultation, stereotactic biopsy planning, and dexamethasone therapy recommended. Staging CT chest/abdomen/pelvis advised to exclude systemic primary.",
  },
  // 20. Normal chest X-ray
  {
    quality: Quality.good,
    qualityReason: "Well-positioned PA chest radiograph of diagnostic quality",
    findings: [
      {
        name: "Clear Lung Fields Bilaterally",
        confidence: 97,
        bbox: { x: 8, y: 18, w: 84, h: 62 },
      },
      {
        name: "Normal Cardiac Silhouette",
        confidence: 95,
        bbox: { x: 33, y: 30, w: 34, h: 40 },
      },
      {
        name: "Normal Mediastinal Contour",
        confidence: 93,
        bbox: { x: 38, y: 10, w: 24, h: 55 },
      },
    ],
    findingsSummary: "No acute cardiopulmonary abnormality identified",
    triageStatus: TriageStatus.normal,
    triageReason: "Normal study — routine follow-up",
    priority: "Standard",
    reportText:
      "CLINICAL INDICATION:\nRoutine pre-operative chest X-ray.\n\nFINDINGS:\n1. Lung fields are clear bilaterally with no consolidation, effusion, or pneumothorax.\n2. Cardiac silhouette within normal limits (cardiothoracic ratio <0.5).\n3. Mediastinum normal in width. Hilar contours unremarkable.\n4. Osseous structures intact. No rib fracture identified.\n\nIMPRESSION:\nNormal chest radiograph. No acute cardiopulmonary process identified. Suitable for pre-operative clearance.",
  },
];

function getMockResult(base64: string, fileName?: string): AnalysisResult {
  // Keyword-based detection from filename for more accurate mock results
  if (fileName) {
    const name = fileName.toLowerCase();
    const brainTumorKeywords = [
      "tumor",
      "tumour",
      "glioma",
      "glioblastoma",
      "gbm",
      "meningioma",
      "metastasis",
      "brain_mass",
      "brain-mass",
      "mass",
    ];
    const brainKeywords = ["brain", "head", "cerebr", "mri_brain", "ct_brain"];
    const fractureKeywords = [
      "fracture",
      "fract",
      "bone",
      "broken",
      "tibia",
      "femur",
      "radius",
      "ulna",
      "humerus",
      "fibula",
      "wrist",
      "ankle",
      "hip",
      "clavicle",
    ];
    const chestKeywords = [
      "chest",
      "lung",
      "thorax",
      "cxr",
      "xray_chest",
      "pneumonia",
    ];
    const spineKeywords = [
      "spine",
      "spinal",
      "vertebra",
      "lumbar",
      "cervical",
      "thoracic",
      "disc",
    ];
    const abdomenKeywords = [
      "abdomen",
      "abdom",
      "liver",
      "kidney",
      "renal",
      "pelvis",
      "ct_abdomen",
    ];

    if (brainTumorKeywords.some((k) => name.includes(k))) {
      // Return the brain tumor case (index 20)
      return MOCK_RESULTS[20];
    }
    if (brainKeywords.some((k) => name.includes(k))) {
      // Return brain hemorrhage (index 6) or normal brain (index 16)
      return MOCK_RESULTS[Math.abs(base64.charCodeAt(0)) % 2 === 0 ? 6 : 16];
    }
    if (fractureKeywords.some((k) => name.includes(k))) {
      // Return bone fracture cases: tibia (4), vertebral (5), shoulder dislocation (18)
      const boneIdx = [4, 5, 18];
      return MOCK_RESULTS[
        boneIdx[Math.abs(base64.charCodeAt(0)) % boneIdx.length]
      ];
    }
    if (chestKeywords.some((k) => name.includes(k))) {
      return MOCK_RESULTS[Math.abs(base64.charCodeAt(0)) % 2 === 0 ? 0 : 19];
    }
    if (spineKeywords.some((k) => name.includes(k))) {
      return MOCK_RESULTS[5];
    }
    if (abdomenKeywords.some((k) => name.includes(k))) {
      return MOCK_RESULTS[15];
    }
  }
  // Fallback: hash-based selection
  let hash = 0;
  const sample = base64.slice(0, 200);
  for (let i = 0; i < sample.length; i++) {
    hash = (hash * 31 + sample.charCodeAt(i)) & 0xffffffff;
  }
  return MOCK_RESULTS[Math.abs(hash) % MOCK_RESULTS.length];
}

async function runAnalysisPipeline(
  base64: string,
  onStep: (step: number) => void,
  fileName?: string,
): Promise<AnalysisResult> {
  const apiKey = localStorage.getItem("gemini_api_key") || "";

  if (!apiKey) return getMockResult(base64, fileName);

  try {
    // Agent 1: Image Quality
    onStep(0);
    const q1 = await geminiImageCall(
      apiKey,
      'You are a radiology AI. Assess this medical image quality regardless of body part or modality. Respond ONLY with JSON (no markdown): {"quality": "good" or "poor", "reason": "brief reason"}',
      base64,
    );
    const quality = safeJsonParse<{ quality: string; reason: string }>(q1);
    onStep(1);

    // Agent 2: Abnormality Detection
    const q2 = await geminiImageCall(
      apiKey,
      'You are a radiology AI assistant. First determine what type of medical image this is (chest X-ray, bone X-ray, brain MRI, CT scan, spine, abdomen, etc.). Then identify ALL visible abnormalities specific to that image type — do NOT default to lung or chest findings if the image shows bones, brain, spine or other anatomy. Respond ONLY with valid JSON (no markdown): {"imageType": "detected image type", "findings": [{"name": "specific finding name relevant to this anatomy", "confidence": 85, "bbox": {"x": 20, "y": 30, "w": 25, "h": 20}}], "summary": "brief summary mentioning the image type and findings"}',
      base64,
    );
    const abnorm = safeJsonParse<{
      findings: FindingResult[];
      summary: string;
      imageType?: string;
    }>(q2);
    onStep(2);

    const detectedFindings =
      abnorm?.findings ?? getMockResult(base64, fileName).findings;
    const findingsList = detectedFindings.map(
      (f) => `${f.name} (${f.confidence}% confidence)`,
    );

    // Agent 3: Triage
    const q3 = await geminiTextCall(
      apiKey,
      `Given these radiology findings from a ${abnorm?.imageType ?? "medical"} image: ${findingsList.join(", ")}, classify the case urgency. Respond ONLY with JSON: {"status": "Critical" or "Normal", "reason": "...", "priority": "Critical-first" or "Standard"}`,
    );
    const triage = safeJsonParse<{
      status: string;
      reason: string;
      priority: string;
    }>(q3);
    onStep(3);

    // Agent 4: Report
    const q4 = await geminiTextCall(
      apiKey,
      `Generate a structured radiology report for a ${abnorm?.imageType ?? "medical"} image with these findings: ${findingsList.join(", ")}. Format with sections: CLINICAL INDICATION, FINDINGS, IMPRESSION. Be professional and concise. Do not add findings that were not detected.`,
    );
    onStep(4);

    return {
      quality: quality?.quality === "poor" ? Quality.poor : Quality.good,
      qualityReason: quality?.reason ?? "Image quality assessed",
      findings: detectedFindings,
      findingsSummary: abnorm?.summary ?? "",
      triageStatus:
        triage?.status?.toLowerCase() === "critical"
          ? TriageStatus.critical
          : TriageStatus.normal,
      triageReason: triage?.reason ?? "",
      priority: triage?.priority ?? "Standard",
      reportText: q4,
    };
  } catch {
    return getMockResult(base64, fileName);
  }
}

// ─── Sub-components ───────────────────────────────────────────────────────────

function TriageBadge({ status }: { status: TriageStatus | string }) {
  const isCrit = status === TriageStatus.critical || status === "critical";
  return (
    <Badge
      className={`text-xs font-semibold px-2 py-0.5 ${
        isCrit
          ? "bg-destructive/20 text-destructive border border-destructive/40"
          : "bg-success/20 text-success border border-success/40"
      }`}
    >
      {isCrit ? (
        <>
          <AlertTriangle className="w-3 h-3 mr-1" />
          CRITICAL
        </>
      ) : (
        <>
          <CheckCircle2 className="w-3 h-3 mr-1" />
          NORMAL
        </>
      )}
    </Badge>
  );
}

function AgentPipelineCard({ steps }: { steps: AgentStep[] }) {
  return (
    <Card className="bg-card border-border shadow-card">
      <CardHeader className="pb-3">
        <CardTitle className="text-sm font-semibold text-foreground flex items-center gap-2">
          <Zap className="w-4 h-4 text-primary" />
          AI Agent Pipeline
        </CardTitle>
      </CardHeader>
      <CardContent className="space-y-2">
        {steps.map((step, i) => (
          <div
            key={step.name}
            className="flex items-center gap-3 p-2.5 rounded-md bg-muted/40"
            data-ocid={`pipeline.item.${i + 1}`}
          >
            <div className="w-6 h-6 rounded-full bg-muted flex items-center justify-center text-xs font-bold text-muted-foreground shrink-0">
              {i + 1}
            </div>
            <div className="flex-1 min-w-0">
              <p className="text-xs font-medium text-foreground truncate">
                {step.name}
              </p>
            </div>
            <div className="shrink-0">
              {step.status === "idle" && (
                <span className="text-xs text-muted-foreground">Waiting</span>
              )}
              {step.status === "processing" && (
                <div className="flex items-center gap-1 text-warning text-xs">
                  <Loader2 className="w-3 h-3 animate-spin" />
                  Processing
                </div>
              )}
              {step.status === "complete" && (
                <div className="flex items-center gap-1 text-success text-xs">
                  <CheckCircle2 className="w-3 h-3" />
                  Complete
                </div>
              )}
            </div>
          </div>
        ))}
      </CardContent>
    </Card>
  );
}

function FindingsCard({ findings }: { findings: FindingResult[] }) {
  const colorClass = (conf: number) => {
    if (conf >= 80) return "text-destructive";
    if (conf >= 60) return "text-warning";
    return "text-success";
  };
  const barColor = (conf: number) => {
    if (conf >= 80) return "bg-destructive";
    if (conf >= 60) return "bg-warning";
    return "bg-success";
  };

  return (
    <Card className="bg-card border-border shadow-card">
      <CardHeader className="pb-3">
        <CardTitle className="text-sm font-semibold text-foreground flex items-center gap-2">
          <Search className="w-4 h-4 text-primary" />
          Findings ({findings.length})
        </CardTitle>
      </CardHeader>
      <CardContent className="space-y-3">
        {findings.length === 0 && (
          <p className="text-muted-foreground text-xs text-center py-2">
            No findings detected
          </p>
        )}
        {findings.map((f, i) => (
          <div key={f.name} data-ocid={`findings.item.${i + 1}`}>
            <div className="flex items-center justify-between mb-1">
              <span className="text-xs font-medium text-foreground truncate max-w-[65%]">
                {f.name}
              </span>
              <span className={`text-xs font-bold ${colorClass(f.confidence)}`}>
                {f.confidence}%
              </span>
            </div>
            <div className="h-1.5 rounded-full bg-muted overflow-hidden">
              <div
                className={`h-full rounded-full transition-all ${barColor(f.confidence)}`}
                style={{ width: `${f.confidence}%` }}
              />
            </div>
          </div>
        ))}
      </CardContent>
    </Card>
  );
}

function SettingsDialog() {
  const [key, setKey] = useState(
    () => localStorage.getItem("gemini_api_key") || "",
  );
  const [open, setOpen] = useState(false);

  const handleSave = () => {
    localStorage.setItem("gemini_api_key", key);
    toast.success("API key saved");
    setOpen(false);
  };

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogTrigger asChild>
        <Button
          variant="ghost"
          size="sm"
          className="text-muted-foreground hover:text-foreground"
          data-ocid="settings.open_modal_button"
        >
          <Settings className="w-4 h-4" />
        </Button>
      </DialogTrigger>
      <DialogContent
        className="bg-card border-border"
        data-ocid="settings.dialog"
      >
        <DialogHeader>
          <DialogTitle className="text-foreground">Settings</DialogTitle>
        </DialogHeader>
        <div className="space-y-4 pt-2">
          <div className="space-y-2">
            <Label className="text-sm text-foreground">AI API Key</Label>
            <Input
              type="password"
              value={key}
              onChange={(e) => setKey(e.target.value)}
              placeholder="AIza..."
              className="bg-muted border-border text-foreground"
              data-ocid="settings.input"
            />
            <p className="text-xs text-muted-foreground">
              Required for live AI analysis. Without it, mock data is used.
            </p>
          </div>
          <div className="flex gap-2 justify-end">
            <Button
              variant="outline"
              onClick={() => setOpen(false)}
              className="border-border"
              data-ocid="settings.cancel_button"
            >
              Cancel
            </Button>
            <Button onClick={handleSave} data-ocid="settings.save_button">
              Save Key
            </Button>
          </div>
        </div>
      </DialogContent>
    </Dialog>
  );
}

// ─── Views ────────────────────────────────────────────────────────────────────

// Preset doctor credentials for demo
const DOCTOR_CREDENTIALS: Record<
  string,
  { password: string; name: string; specialty: string }
> = {
  DR001: {
    password: "radio@2024",
    name: "Dr. Sarah Chen",
    specialty: "Radiology",
  },
  DR002: {
    password: "neuro@2024",
    name: "Dr. James Patel",
    specialty: "Neurology",
  },
  DR003: {
    password: "cardio@2024",
    name: "Dr. Maria Rossi",
    specialty: "Cardiology",
  },
  DR004: {
    password: "ortho@2024",
    name: "Dr. Ahmed Khan",
    specialty: "Orthopedics",
  },
};

function getStoredPasswords(): Record<string, string> {
  try {
    const raw = localStorage.getItem("doctor_passwords");
    return raw ? JSON.parse(raw) : {};
  } catch {
    return {};
  }
}

function savePassword(doctorId: string, password: string) {
  const stored = getStoredPasswords();
  stored[doctorId] = password;
  localStorage.setItem("doctor_passwords", JSON.stringify(stored));
}

function LoginView({
  onLogin,
  isLoggingIn,
}: { onLogin: () => void; isLoggingIn: boolean }) {
  const [doctorId, setDoctorId] = useState("");
  const [password, setPassword] = useState("");
  const [confirmPassword, setConfirmPassword] = useState("");
  const [error, setError] = useState("");
  const [credVerified, setCredVerified] = useState(false);
  const [step, setStep] = useState<
    "enterCredentials" | "setPassword" | "verified"
  >("enterCredentials");
  const [isNewDoctor, setIsNewDoctor] = useState(false);
  const [displayName, setDisplayName] = useState("");
  const [displaySpecialty, setDisplaySpecialty] = useState("");

  const handleIdSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    setError("");
    const id = doctorId.trim().toUpperCase();
    const hardcoded = DOCTOR_CREDENTIALS[id];
    const stored = getStoredPasswords();

    if (hardcoded) {
      setIsNewDoctor(false);
      setDisplayName(hardcoded.name);
      setDisplaySpecialty(hardcoded.specialty);
      setStep("enterCredentials");
    } else if (stored[id]) {
      setIsNewDoctor(false);
      setDisplayName(id);
      setDisplaySpecialty("Doctor");
      setStep("enterCredentials");
    } else {
      setIsNewDoctor(true);
      setDisplayName(id);
      setDisplaySpecialty("Doctor");
      setStep("setPassword");
    }
  };

  const handlePasswordSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    setError("");
    const id = doctorId.trim().toUpperCase();
    const hardcoded = DOCTOR_CREDENTIALS[id];
    const stored = getStoredPasswords();

    if (isNewDoctor) {
      if (password.length < 6) {
        setError("Password must be at least 6 characters.");
        return;
      }
      if (password !== confirmPassword) {
        setError("Passwords do not match.");
        return;
      }
      savePassword(id, password);
      setCredVerified(true);
      setStep("verified");
    } else {
      const expectedPassword = hardcoded ? hardcoded.password : stored[id];
      if (!expectedPassword || password !== expectedPassword) {
        setError("Incorrect password. Please try again.");
        return;
      }
      setCredVerified(true);
      setStep("verified");
    }
  };

  const isIdStep =
    step === "enterCredentials" &&
    !credVerified &&
    !isNewDoctor &&
    displayName === "";

  return (
    <div
      className="min-h-screen flex items-center justify-center"
      style={{
        background:
          "linear-gradient(135deg, oklch(0.135 0.031 244) 0%, oklch(0.18 0.035 240) 100%)",
      }}
    >
      <motion.div
        initial={{ opacity: 0, y: 24 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.5 }}
        className="w-full max-w-sm mx-4"
      >
        <Card className="bg-card border-border shadow-card">
          <CardContent className="pt-8 pb-8 px-8">
            <div className="text-center mb-6">
              <div className="w-16 h-16 rounded-2xl bg-primary/20 border border-primary/30 flex items-center justify-center mx-auto mb-4">
                <Brain className="w-8 h-8 text-primary" />
              </div>
              <h1 className="text-2xl font-bold text-foreground tracking-tight">
                Radioagent
              </h1>
              <p className="text-muted-foreground text-sm mt-2">
                AI-Powered Radiology Assistant
              </p>
              <p className="text-muted-foreground/70 text-xs mt-1">
                4-agent pipeline · Multi-agent AI
              </p>
            </div>

            {step === "verified" ? (
              <div className="space-y-4">
                <div className="flex items-center gap-2 bg-primary/10 border border-primary/20 rounded-md px-3 py-2.5">
                  <Shield className="w-4 h-4 text-primary shrink-0" />
                  <div>
                    <p className="text-xs font-medium text-foreground">
                      {displayName}
                    </p>
                    <p className="text-xs text-muted-foreground">
                      {displaySpecialty} · {doctorId.toUpperCase()}
                    </p>
                  </div>
                </div>
                <p className="text-xs text-center text-muted-foreground">
                  Complete sign-in with your secure identity
                </p>
                <Button
                  onClick={onLogin}
                  disabled={isLoggingIn}
                  className="w-full bg-primary hover:bg-primary/90 text-primary-foreground font-semibold"
                  data-ocid="login.primary_button"
                >
                  {isLoggingIn ? (
                    <>
                      <Loader2 className="mr-2 h-4 w-4 animate-spin" />
                      Connecting...
                    </>
                  ) : (
                    "Complete Login"
                  )}
                </Button>
                <button
                  type="button"
                  onClick={() => {
                    setCredVerified(false);
                    setPassword("");
                    setConfirmPassword("");
                    setError("");
                    setStep("enterCredentials");
                    setDisplayName("");
                    setIsNewDoctor(false);
                  }}
                  className="w-full text-xs text-muted-foreground/60 hover:text-muted-foreground text-center pt-1"
                >
                  ← Use different credentials
                </button>
              </div>
            ) : isIdStep ? (
              <form onSubmit={handleIdSubmit} className="space-y-4">
                <div className="space-y-1.5">
                  <Label
                    htmlFor="doctor-id"
                    className="text-xs text-muted-foreground"
                  >
                    Doctor ID
                  </Label>
                  <Input
                    id="doctor-id"
                    placeholder="e.g. DR001"
                    value={doctorId}
                    onChange={(e) => setDoctorId(e.target.value)}
                    className="bg-background/50 border-border/60 text-sm"
                    autoComplete="username"
                    required
                    data-ocid="login.input"
                  />
                </div>
                <Button
                  type="submit"
                  className="w-full bg-primary hover:bg-primary/90 text-primary-foreground font-semibold mt-2"
                >
                  Continue
                </Button>
                <p className="text-center text-xs text-muted-foreground/50 pt-1">
                  Demo IDs: DR001–DR004
                </p>
              </form>
            ) : step === "setPassword" ? (
              <form onSubmit={handlePasswordSubmit} className="space-y-4">
                <div className="bg-blue-500/10 border border-blue-500/20 rounded-md px-3 py-2 mb-1">
                  <p className="text-xs text-blue-400 font-medium">
                    New account detected
                  </p>
                  <p className="text-xs text-muted-foreground mt-0.5">
                    Please create your password for{" "}
                    <span className="font-semibold">
                      {doctorId.toUpperCase()}
                    </span>
                  </p>
                </div>
                <div className="space-y-1.5">
                  <Label
                    htmlFor="new-password"
                    className="text-xs text-muted-foreground"
                  >
                    New Password
                  </Label>
                  <Input
                    id="new-password"
                    type="password"
                    placeholder="Min. 6 characters"
                    value={password}
                    onChange={(e) => setPassword(e.target.value)}
                    className="bg-background/50 border-border/60 text-sm"
                    autoComplete="new-password"
                    required
                    data-ocid="login.input"
                  />
                </div>
                <div className="space-y-1.5">
                  <Label
                    htmlFor="confirm-password"
                    className="text-xs text-muted-foreground"
                  >
                    Confirm Password
                  </Label>
                  <Input
                    id="confirm-password"
                    type="password"
                    placeholder="Re-enter password"
                    value={confirmPassword}
                    onChange={(e) => setConfirmPassword(e.target.value)}
                    className="bg-background/50 border-border/60 text-sm"
                    autoComplete="new-password"
                    required
                    data-ocid="login.input"
                  />
                </div>
                {error && (
                  <p className="text-xs text-destructive bg-destructive/10 border border-destructive/20 rounded-md px-3 py-2">
                    {error}
                  </p>
                )}
                <Button
                  type="submit"
                  className="w-full bg-primary hover:bg-primary/90 text-primary-foreground font-semibold mt-2"
                >
                  <Shield className="mr-2 h-4 w-4" />
                  Create Password
                </Button>
                <button
                  type="button"
                  onClick={() => {
                    setStep("enterCredentials");
                    setDisplayName("");
                    setIsNewDoctor(false);
                    setPassword("");
                    setConfirmPassword("");
                    setError("");
                  }}
                  className="w-full text-xs text-muted-foreground/60 hover:text-muted-foreground text-center pt-1"
                >
                  ← Back
                </button>
              </form>
            ) : (
              <form onSubmit={handlePasswordSubmit} className="space-y-4">
                <div className="space-y-1.5">
                  <Label
                    htmlFor="doctor-id-show"
                    className="text-xs text-muted-foreground"
                  >
                    Doctor ID
                  </Label>
                  <Input
                    id="doctor-id-show"
                    value={doctorId.toUpperCase()}
                    readOnly
                    className="bg-background/30 border-border/40 text-sm text-muted-foreground"
                  />
                </div>
                <div className="space-y-1.5">
                  <Label
                    htmlFor="doctor-password"
                    className="text-xs text-muted-foreground"
                  >
                    Password
                  </Label>
                  <Input
                    id="doctor-password"
                    type="password"
                    placeholder="Enter your password"
                    value={password}
                    onChange={(e) => setPassword(e.target.value)}
                    className="bg-background/50 border-border/60 text-sm"
                    autoComplete="current-password"
                    required
                    data-ocid="login.input"
                  />
                </div>
                {error && (
                  <p className="text-xs text-destructive bg-destructive/10 border border-destructive/20 rounded-md px-3 py-2">
                    {error}
                  </p>
                )}
                <Button
                  type="submit"
                  className="w-full bg-primary hover:bg-primary/90 text-primary-foreground font-semibold mt-2"
                >
                  <Shield className="mr-2 h-4 w-4" />
                  Verify Credentials
                </Button>
                <button
                  type="button"
                  onClick={() => {
                    setStep("enterCredentials");
                    setDisplayName("");
                    setPassword("");
                    setError("");
                  }}
                  className="w-full text-xs text-muted-foreground/60 hover:text-muted-foreground text-center pt-1"
                >
                  ← Back
                </button>
              </form>
            )}
          </CardContent>
        </Card>
      </motion.div>
    </div>
  );
}

interface UploadPanelProps {
  onAnalyze: (
    file: File,
    patientName: string,
    modality: ModalityUI,
    patientPhone: string,
  ) => void;
  isAnalyzing: boolean;
  agentSteps: AgentStep[];
}

function UploadPanel({ onAnalyze, isAnalyzing, agentSteps }: UploadPanelProps) {
  const [dragOver, setDragOver] = useState(false);
  const [file, setFile] = useState<File | null>(null);
  const [preview, setPreview] = useState<string | null>(null);
  const [patientName, setPatientName] = useState("");
  const [patientPhone, setPatientPhone] = useState("");
  const [modality, setModality] = useState<ModalityUI>("xray");
  const fileInputRef = useRef<HTMLInputElement>(null);

  const handleFile = (f: File) => {
    setFile(f);
    const url = URL.createObjectURL(f);
    setPreview(url);
  };

  const handleDrop = (e: React.DragEvent) => {
    e.preventDefault();
    setDragOver(false);
    const f = e.dataTransfer.files[0];
    if (f) handleFile(f);
  };

  const handleAnalyze = () => {
    if (!file) {
      toast.error("Please select a medical image");
      return;
    }
    if (!patientName.trim()) {
      toast.error("Please enter patient name");
      return;
    }
    if (!patientPhone.trim()) {
      toast.error("Please enter patient phone number");
      return;
    }
    onAnalyze(file, patientName, modality, patientPhone);
  };

  return (
    <div className="flex-1 p-6 overflow-auto">
      <motion.div
        initial={{ opacity: 0, y: 16 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.4 }}
        className="max-w-5xl mx-auto"
      >
        <div className="mb-6">
          <h2 className="text-xl font-bold text-foreground">New Analysis</h2>
          <p className="text-muted-foreground text-sm mt-1">
            Upload a medical image to begin AI-powered analysis
          </p>
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
          {/* Left: Upload + Patient Info */}
          <div className="space-y-4">
            {/* Drop zone */}
            <div
              className={`relative border-2 border-dashed rounded-xl p-8 text-center cursor-pointer transition-all ${
                dragOver
                  ? "border-primary bg-primary/10"
                  : "border-border hover:border-primary/50 hover:bg-muted/30"
              }`}
              onDragOver={(e) => {
                e.preventDefault();
                setDragOver(true);
              }}
              onDragLeave={() => setDragOver(false)}
              onDrop={handleDrop}
              onClick={() => fileInputRef.current?.click()}
              onKeyDown={(e) => {
                if (e.key === "Enter" || e.key === " ")
                  fileInputRef.current?.click();
              }}
              data-ocid="upload.dropzone"
            >
              <input
                ref={fileInputRef}
                type="file"
                accept="image/*,.dcm"
                className="hidden"
                onChange={(e) => {
                  const f = e.target.files?.[0];
                  if (f) handleFile(f);
                }}
                data-ocid="upload.upload_button"
              />
              {preview ? (
                <div className="space-y-3">
                  <img
                    src={preview}
                    alt="Preview"
                    className="max-h-40 mx-auto rounded-lg object-contain"
                  />
                  <p className="text-xs text-muted-foreground">{file?.name}</p>
                  <p className="text-xs text-primary">Click to change image</p>
                </div>
              ) : (
                <div className="space-y-3">
                  <div className="w-12 h-12 rounded-full bg-muted flex items-center justify-center mx-auto">
                    <Upload className="w-6 h-6 text-muted-foreground" />
                  </div>
                  <div>
                    <p className="text-sm font-medium text-foreground">
                      Drop medical image here
                    </p>
                    <p className="text-xs text-muted-foreground mt-1">
                      or click to browse
                    </p>
                  </div>
                  <p className="text-xs text-muted-foreground">
                    Supports DICOM, JPEG, PNG
                  </p>
                </div>
              )}
            </div>

            {/* Patient info */}
            <div className="space-y-3">
              <div>
                <Label className="text-xs font-medium text-muted-foreground uppercase tracking-wide mb-1.5 block">
                  Patient Name
                </Label>
                <Input
                  value={patientName}
                  onChange={(e) => setPatientName(e.target.value)}
                  placeholder="Enter patient full name"
                  className="bg-card border-border text-foreground"
                  data-ocid="upload.input"
                />
              </div>
              <div>
                <Label className="text-xs font-medium text-muted-foreground uppercase tracking-wide mb-1.5 block">
                  Patient Phone Number
                </Label>
                <Input
                  value={patientPhone}
                  onChange={(e) => setPatientPhone(e.target.value)}
                  placeholder="e.g. +1 555 123 4567"
                  type="tel"
                  className="bg-card border-border text-foreground"
                />
              </div>
              <div>
                <Label className="text-xs font-medium text-muted-foreground uppercase tracking-wide mb-1.5 block">
                  Imaging Modality
                </Label>
                <Select
                  value={modality}
                  onValueChange={(v) => setModality(v as ModalityUI)}
                >
                  <SelectTrigger
                    className="bg-card border-border text-foreground"
                    data-ocid="upload.select"
                  >
                    <SelectValue />
                  </SelectTrigger>
                  <SelectContent className="bg-card border-border">
                    <SelectItem value="xray">X-Ray</SelectItem>
                    <SelectItem value="ct">CT Scan</SelectItem>
                    <SelectItem value="mri">MRI</SelectItem>
                  </SelectContent>
                </Select>
              </div>
            </div>

            {/* Analyze button */}
            <Button
              onClick={handleAnalyze}
              disabled={isAnalyzing || !file}
              className="w-full bg-primary hover:bg-primary/90 text-primary-foreground font-semibold py-5"
              data-ocid="upload.submit_button"
            >
              {isAnalyzing ? (
                <>
                  <Loader2 className="mr-2 h-4 w-4 animate-spin" />
                  Analyzing...
                </>
              ) : (
                <>
                  <Brain className="mr-2 h-4 w-4" />
                  Analyze Scan
                </>
              )}
            </Button>

            {/* Pipeline progress during analysis */}
            <AnimatePresence>
              {isAnalyzing && (
                <motion.div
                  initial={{ opacity: 0, height: 0 }}
                  animate={{ opacity: 1, height: "auto" }}
                  exit={{ opacity: 0, height: 0 }}
                >
                  <AgentPipelineCard steps={agentSteps} />
                </motion.div>
              )}
            </AnimatePresence>
          </div>

          {/* Right: Agent info */}
          <div className="space-y-4">
            <Card className="bg-card border-border shadow-card">
              <CardHeader className="pb-3">
                <CardTitle className="text-sm font-semibold text-foreground flex items-center gap-2">
                  <Brain className="w-4 h-4 text-primary" />
                  Multi-Agent AI Pipeline
                </CardTitle>
              </CardHeader>
              <CardContent className="space-y-4">
                {[
                  {
                    icon: <Activity className="w-4 h-4 text-primary" />,
                    title: "Agent 1 — Image Quality",
                    desc: "Evaluates image clarity, resolution, and diagnostic suitability before analysis.",
                  },
                  {
                    icon: <Search className="w-4 h-4 text-warning" />,
                    title: "Agent 2 — Abnormality Detection",
                    desc: "Identifies fractures, tumors, pneumonia, and other pathologies with bounding boxes.",
                  },
                  {
                    icon: <Shield className="w-4 h-4 text-destructive" />,
                    title: "Agent 3 — Priority Triage",
                    desc: "Classifies case urgency and assigns Critical-first status for life-threatening findings.",
                  },
                  {
                    icon: <FileText className="w-4 h-4 text-success" />,
                    title: "Agent 4 — Report Generation",
                    desc: "Drafts a structured, professional radiology report with clinical indication and impression.",
                  },
                ].map((agent) => (
                  <div
                    key={agent.title}
                    className="flex gap-3 p-3 rounded-lg bg-muted/30 border border-border/50"
                  >
                    <div className="mt-0.5 shrink-0">{agent.icon}</div>
                    <div>
                      <p className="text-xs font-semibold text-foreground">
                        {agent.title}
                      </p>
                      <p className="text-xs text-muted-foreground mt-0.5 leading-relaxed">
                        {agent.desc}
                      </p>
                    </div>
                  </div>
                ))}
              </CardContent>
            </Card>

            <Card className="bg-card border-border shadow-card">
              <CardContent className="pt-4 pb-4">
                <div className="flex items-start gap-3">
                  <Zap className="w-4 h-4 text-warning mt-0.5 shrink-0" />
                  <div>
                    <p className="text-xs font-semibold text-foreground">
                      AI-Powered Analysis
                    </p>
                    <p className="text-xs text-muted-foreground mt-1">
                      Set your API key in Settings for live analysis. Without a
                      key, realistic demo data is shown.
                    </p>
                  </div>
                </div>
              </CardContent>
            </Card>
          </div>
        </div>
      </motion.div>
    </div>
  );
}

interface AnalysisDashboardProps {
  result: AnalysisResult;
  imageUrl: string;
  patientName: string;
  patientPhone: string;
  modality: ModalityUI;
  agentSteps: AgentStep[];
  pastAnalyses: AnalysisRecord[];
  onNewAnalysis: () => void;
  isDemoMode: boolean;
  onAlertSent: (id: string) => void;
}

function AnalysisDashboard({
  result,
  imageUrl,
  patientName,
  patientPhone,
  modality,
  agentSteps,
  pastAnalyses,
  onNewAnalysis,
  isDemoMode,
  onAlertSent,
}: AnalysisDashboardProps) {
  const [reportText, setReportText] = useState(result.reportText);
  const cid =
    result.submittedId !== undefined ? caseId(result.submittedId) : "RA-DRAFT";

  return (
    <div className="flex-1 p-4 overflow-auto">
      <motion.div
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ duration: 0.4 }}
        className="space-y-4"
      >
        {/* Demo mode notice */}
        {isDemoMode && (
          <div
            className="flex items-center gap-2 px-3 py-2 rounded-lg bg-amber-500/10 border border-amber-500/30 text-amber-600 dark:text-amber-400 text-xs"
            data-ocid="analysis.panel"
          >
            <span className="font-semibold">⚠ Demo mode:</span>
            <span>
              Results are illustrative only. Add your AI API key in Settings for
              real AI analysis.
            </span>
          </div>
        )}

        {/* Case header bar */}
        <div className="flex items-center justify-between p-3 rounded-xl bg-card border border-border">
          <div className="flex items-center gap-3">
            <span className="text-xs font-mono text-muted-foreground">
              Case ID
            </span>
            <span className="text-sm font-bold text-foreground">{cid}</span>
            <TriageBadge status={result.triageStatus} />
            {result.priority === "Critical-first" && (
              <Badge className="text-xs bg-destructive/15 text-destructive border border-destructive/30">
                CRITICAL-FIRST
              </Badge>
            )}
          </div>
          <Button
            variant="outline"
            size="sm"
            onClick={onNewAnalysis}
            className="border-border text-muted-foreground hover:text-foreground text-xs"
            data-ocid="analysis.secondary_button"
          >
            New Analysis
          </Button>
        </div>

        {/* WhatsApp critical alert banner */}
        {(result.triageStatus === TriageStatus.critical ||
          String(result.triageStatus).toLowerCase() === "critical") &&
          (() => {
            const cleanPhone = patientPhone.replace(/[^0-9]/g, "");
            const phoneWithCountry = cleanPhone.startsWith("91")
              ? cleanPhone
              : `91${cleanPhone}`;
            const waMsg = encodeURIComponent(
              "⚠️ Emergency Alert: Your health condition is critical. Please seek immediate medical attention.",
            );
            const waUrl = `https://wa.me/${phoneWithCountry}?text=${waMsg}`;
            return (
              <div className="flex flex-col gap-2 px-4 py-3 rounded-xl bg-red-50 border-2 border-red-500">
                <div className="flex items-center gap-2 text-red-700 text-sm font-bold">
                  <AlertTriangle className="w-4 h-4" />
                  CRITICAL CASE — {patientName} ({patientPhone})
                </div>
                <div className="flex items-center justify-between gap-4">
                  <p className="text-red-600 text-xs">
                    ⚠️ Click the button → WhatsApp will open → Press{" "}
                    <strong>Send</strong> inside WhatsApp to deliver the
                    message.
                  </p>
                  <a
                    href={waUrl}
                    target="_blank"
                    rel="noreferrer"
                    onClick={() =>
                      result.submittedId !== undefined &&
                      onAlertSent(String(result.submittedId))
                    }
                    className="shrink-0 inline-flex items-center gap-1.5 bg-green-600 hover:bg-green-700 text-white text-xs font-semibold px-3 py-2 rounded-md"
                  >
                    📱 Send Alert via WhatsApp
                  </a>
                </div>
              </div>
            );
          })()}

        {/* 3-column grid */}
        <div className="grid grid-cols-1 xl:grid-cols-3 gap-4">
          {/* Left: Scan Viewer */}
          <Card
            className="bg-card border-border shadow-card"
            data-ocid="analysis.panel"
          >
            <CardHeader className="pb-2">
              <CardTitle className="text-sm font-semibold text-foreground flex items-center gap-2">
                <Activity className="w-4 h-4 text-primary" />
                Scan Viewer
              </CardTitle>
            </CardHeader>
            <CardContent className="p-0 overflow-hidden rounded-b-lg">
              <ScanViewer imageUrl={imageUrl} findings={result.findings} />
              <div className="p-3 border-t border-border">
                <p className="text-xs text-muted-foreground">
                  Quality:
                  <span
                    className={`ml-1 font-semibold ${
                      result.quality === Quality.good
                        ? "text-success"
                        : "text-destructive"
                    }`}
                  >
                    {result.quality === Quality.good ? "Good" : "Poor"}
                  </span>
                  <span className="text-muted-foreground/60 ml-1">
                    — {result.qualityReason}
                  </span>
                </p>
              </div>
            </CardContent>
          </Card>

          {/* Middle: Pipeline + Findings */}
          <div className="space-y-4">
            <AgentPipelineCard steps={agentSteps} />
            <FindingsCard findings={result.findings} />
          </div>

          {/* Right: Case details + history */}
          <div className="space-y-4">
            <Card className="bg-card border-border shadow-card">
              <CardHeader className="pb-2">
                <CardTitle className="text-sm font-semibold text-foreground">
                  Case Details
                </CardTitle>
              </CardHeader>
              <CardContent className="space-y-2.5">
                {[
                  { label: "Patient", value: patientName },
                  { label: "Phone", value: patientPhone || "—" },
                  { label: "Modality", value: modalityLabel(modality) },
                  { label: "Date", value: new Date().toLocaleDateString() },
                  {
                    label: "Status",
                    value: <TriageBadge status={result.triageStatus} />,
                  },
                ].map((row) => (
                  <div
                    key={row.label}
                    className="flex items-center justify-between"
                  >
                    <span className="text-xs text-muted-foreground">
                      {row.label}
                    </span>
                    {typeof row.value === "string" ? (
                      <span className="text-xs font-medium text-foreground">
                        {row.value}
                      </span>
                    ) : (
                      row.value
                    )}
                  </div>
                ))}
              </CardContent>
            </Card>

            {/* Recent cases */}
            <Card className="bg-card border-border shadow-card">
              <CardHeader className="pb-2">
                <CardTitle className="text-sm font-semibold text-foreground flex items-center gap-2">
                  <History className="w-3.5 h-3.5 text-muted-foreground" />
                  Recent Cases
                </CardTitle>
              </CardHeader>
              <CardContent className="space-y-1.5">
                {pastAnalyses.length === 0 && (
                  <p
                    className="text-xs text-muted-foreground text-center py-2"
                    data-ocid="recent.empty_state"
                  >
                    No previous analyses
                  </p>
                )}
                {pastAnalyses.slice(0, 5).map((r, i) => (
                  <div
                    key={String(r.id)}
                    className="flex items-center justify-between p-2 rounded-md hover:bg-muted/30 transition-colors"
                    data-ocid={`recent.item.${i + 1}`}
                  >
                    <div className="min-w-0">
                      <p className="text-xs font-mono text-muted-foreground">
                        {caseId(r.id)}
                      </p>
                      <p className="text-xs text-foreground truncate">
                        {r.patientName}
                      </p>
                    </div>
                    <TriageBadge status={r.triageStatus} />
                  </div>
                ))}
              </CardContent>
            </Card>
          </div>
        </div>

        {/* Bottom: Report */}
        <Card
          className="bg-card border-border shadow-card"
          data-ocid="report.panel"
        >
          <CardHeader className="pb-3">
            <div className="flex items-center justify-between">
              <CardTitle className="text-sm font-semibold text-foreground flex items-center gap-2">
                <FileText className="w-4 h-4 text-primary" />
                Structured Radiology Report
              </CardTitle>
              <div className="flex items-center gap-2">
                <Button
                  size="sm"
                  className="bg-primary hover:bg-primary/90 text-primary-foreground text-xs h-7"
                  data-ocid="report.primary_button"
                  onClick={() => toast.success("Report approved")}
                >
                  Approve Report
                </Button>
                <Button
                  size="sm"
                  variant="outline"
                  className="border-destructive/50 text-destructive hover:bg-destructive/10 text-xs h-7"
                  data-ocid="report.delete_button"
                  onClick={() => setReportText("")}
                >
                  Reject &amp; Edit
                </Button>
                <Button
                  size="sm"
                  variant="outline"
                  className="border-border text-muted-foreground text-xs h-7"
                  data-ocid="report.secondary_button"
                  onClick={() => toast.info("Assigned to MD")}
                >
                  Assign to MD
                </Button>
              </div>
            </div>
          </CardHeader>
          <CardContent>
            <Textarea
              value={reportText}
              onChange={(e) => setReportText(e.target.value)}
              className="bg-muted/30 border-border text-foreground text-xs font-mono min-h-40 resize-y leading-relaxed"
              data-ocid="report.textarea"
            />
          </CardContent>
        </Card>
      </motion.div>
    </div>
  );
}

